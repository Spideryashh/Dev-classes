* first go to main folder 

* write command: 
# npm install cheerio   // it will install node_modules and package-lock.json.
# npm init -y           // it will install package.json.

# make .gitignore folder and write node_modules in it does  not push it on git.
